// 19. Crie um array de números e use every() para verificar se todos os números são positivos.

let numeros = [1, 3, 5, 7];

let numeropositivo = numeros.every(numeros => numeros > 0);

console.log(numeropositivo);